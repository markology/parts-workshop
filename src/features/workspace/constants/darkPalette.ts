/**
 * @deprecated Use the new theme system from @/features/workspace/constants/theme instead
 * This file is kept for backward compatibility but will be removed in the future.
 */
import { darkTheme } from "./theme";

export const workspaceDarkPalette = {
  base: darkTheme.card,
  surface: darkTheme.surface,
  elevated: darkTheme.elevated,
  highlight: darkTheme.accent,
};


